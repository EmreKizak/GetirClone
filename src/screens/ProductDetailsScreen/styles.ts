import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  detaylar: {
    backgroundColor: '#808B99',
    paddingHorizontal: 10,
    paddingVertical: 12,
    fontWeight: '600',
  },
});

export default styles;
